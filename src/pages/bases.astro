---
import BasesPage from '../pages-react/BasesPage/BasesPage';
import Layout from '../layouts/Layout.astro';

const session = Astro.cookies.get('session');
if (!session) {
  return Astro.redirect('/login');
}
---

<Layout title="Bases de la Informática - Academia Web">
  <BasesPage client:load />
</Layout>
