---
import Layout from '../../layouts/Layout.astro';

const session = Astro.cookies.get('session');
if (!session) {
  return Astro.redirect('/login');
}

// Redirect /history/www to /history with web category selected
return Astro.redirect('/history');
---
