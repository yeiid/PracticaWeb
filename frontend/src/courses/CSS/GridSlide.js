import React from 'react';
import styles from './CSSSlides.module.css';

const GridSlide = () => {
  return (
    <div className={styles.slide}>
      <h1>CSS Grid</h1>
      <p>Contenido para CSS Grid...</p>
    </div>
  );
};

export default GridSlide;
